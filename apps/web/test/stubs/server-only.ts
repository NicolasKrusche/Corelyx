// Vitest stub for Next.js's "server-only" marker import.
// Next vendors the real package at build time; in unit tests the poison
// import is irrelevant, so it resolves to this empty module instead.
export {};
